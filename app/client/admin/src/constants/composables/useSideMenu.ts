import { IconLayout4blocks, IconUser, IconRoute, IconLibrary, IconHome, IconAirpods, IconCupboard } from '@starter-core/icons';

export const SIDEMENU_ICONS_MAP: Record<string, any> = {
  dashboard: IconLayout4blocks,
  users: IconUser,
  venues: IconHome,
  events: IconAirpods,
  'post-slots': IconCupboard,
  'navigations.trigger': IconRoute,
  'navigations.menus': IconLibrary,
};
